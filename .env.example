DATABASE_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Signs the session cookie. REQUIRED — the app throws on startup without it.
# Generate with: openssl rand -base64 48
# Changing this value logs every user out.
SESSION_SECRET=
